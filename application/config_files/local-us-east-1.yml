environment: local
serviceName: "eq"
awsRegion: "us-east-1"
scheduleIntervalSeconds: 2000
excel:
  apiKey: "{SECRET:local-googleAPIKey}"
  apiToken: "{SECRET:local-googleAPIToken}"
mongo:
  dbName: eq
  username:
  password:
  uri: mongodb://localhost:27017